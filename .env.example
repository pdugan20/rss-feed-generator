# Server Configuration
PORT=3000
HOST=0.0.0.0

# Your deployed app URL (update this after deploying to Railway)
BASE_URL=https://your-app.up.railway.app

# Environment
NODE_ENV=production

# API Key for manual refresh endpoint
# IMPORTANT: Generate a secure random key for production!
# You can generate one with: openssl rand -base64 32
API_KEY=change-this-to-a-secure-random-string

# Persistent data directory (Railway volume mount path in production)
# DATA_DIR=/app/data